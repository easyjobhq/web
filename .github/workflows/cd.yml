name: CD Pipeline
on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types:
      - completed
jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Pull Docker image
      run: sudo docker pull juanjodiazzz/easyjob-frontend-cicd-pipeline:latest

    - name: Delete old Docker container
      run: sudo docker rm -f easyjob-frontend-cicd-container || true

    - name: Verify environment variables
      env:
        JWT_SECRET: ${{ secrets.JWT_SECRET }}
        NEXT_PUBLIC_BACKEND_URL: ${{ secrets.NEXT_PUBLIC_BACKEND_URL }}
      run: |
        echo "JWT_SECRET is set: ${{ secrets.JWT_SECRET != '' }}"
        echo "NEXT_PUBLIC_BACKEND_URL is set: ${{ secrets.NEXT_PUBLIC_BACKEND_URL != '' }}"
        echo "NEXT_PUBLIC_BACKEND_URL: $NEXT_PUBLIC_BACKEND_URL"

    - name: Run Docker container 
      env:
        JWT_SECRET: ${{ secrets.JWT_SECRET }}
        NEXT_PUBLIC_BACKEND_URL: ${{ secrets.NEXT_PUBLIC_BACKEND_URL }}
      run: |
        sudo docker run -d -p 80:3000 --name easyjob-frontend-cicd-container \
<<<<<<< HEAD
        -e JWT_SECRET=$JWT_SECRET \
        -e NEXT_PUBLIC_BACKEND_URL=$NEXT_PUBLIC_BACKEND_URL \
        juanjodiazzz/easyjob-frontend-cicd-pipeline:latest

    - name: Verify environment variables inside Docker container
      run: |
        sudo docker exec easyjob-frontend-cicd-container printenv NEXT_PUBLIC_BACKEND_URL
=======
        -e JWT_SECRET="${JWT_SECRET}" \
        -e NEXT_PUBLIC_BACKEND_URL="${NEXT_PUBLIC_BACKEND_URL}" \
        juanjodiazzz/easyjob-frontend-cicd-pipeline:latest

    - name: Verify container environment
      run: |
        echo "Container environment variables:"
        sudo docker exec easyjob-frontend-cicd-container env | grep -E 'JWT_SECRET|NEXT_PUBLIC_BACKEND_URL'
>>>>>>> main
